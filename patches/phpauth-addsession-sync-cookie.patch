--- a/sources/Auth.php
+++ b/sources/Auth.php
@@ -1431,6 +1431,9 @@
         } else {
             if (!setcookie($this->config->cookie_name, $data['hash'], $cookie_options)) {
                 return false;
             }
+            // Same-request read: removeCookie() may have unset $_COOKIE during session renewal in
+            // checkSession(); Set-Cookie does not populate $_COOKIE until the next HTTP request.
+            $_COOKIE[$this->config->cookie_name] = $data['hash'];
         }
 
         return $data;
