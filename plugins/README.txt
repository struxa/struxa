Plugins are not versioned in this repo. Keep local copies here for development if needed,
or install from Admin → Plugins → Browse catalog. Only plugins/.gitkeep is committed.

To bundle plugins in git again (monorepo), add plugins/.struxa-bundle-plugins and commit plugin folders.
