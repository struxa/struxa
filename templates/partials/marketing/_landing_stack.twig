<section class="section" id="stack">
  <div class="split">
    <div class="split-copy reveal" data-reveal>
      <p class="section-kicker section-kicker--left">Under the hood</p>
      <h2>Built with the stack buyers expect</h2>
      <p>
        The marketing site is served with PHP, Slim, Twig, and PHPAuth-backed accounts; the page layer stays
        semantic HTML, layered CSS (custom properties, container queries where supported), and small JavaScript:
        observers, springy metrics, and a lightweight canvas accent — no heavy SPA framework on the landing.
      </p>
      <div class="pill-row">
        <span class="pill pill--violet">IntersectionObserver</span>
        <span class="pill pill--cyan">CSS @property</span>
        <span class="pill pill--amber">requestAnimationFrame</span>
        <span class="pill pill--rose">prefers-reduced-motion</span>
      </div>
    </div>
    <div class="split-visual reveal" data-reveal>
      <div class="glass-panel">
        <div class="code-block" aria-hidden="true">
          <div class="code-line"><span class="kw">const</span> <span class="id">ready</span> = <span class="str">await</span> ship(<span class="str">'your-idea'</span>);</div>
          <div class="code-line"><span class="kw">export</span> { <span class="id">ready</span> <span class="kw">as</span> <span class="id">default</span> };</div>
        </div>
        <p class="glass-caption">Replace copy, swap assets, wire your checkout — the layout stays sharp.</p>
      </div>
    </div>
  </div>
</section>
