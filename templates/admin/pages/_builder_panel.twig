{# Page builder panel — delegates to shared block builder #}
{% include 'admin/sections/_builder_panel.twig' with {
  builder_title: 'Page builder',
  builder_lead: 'Compose the page with reusable blocks. Drag to reorder — order saves automatically.',
  builder_action_url: url_for('admin.pages.builder', { id: page.id }),
  builder_section_edit_route: 'admin.pages.builder.section_edit',
  builder_route_params: { id: page.id },
  builder_settings_url: builder_panel_standalone|default(false) ? url_for('admin.pages.edit', { id: page.id }) ~ '#page-builder' : null,
  builder_settings_label: 'Page settings',
  builder_full_builder_url: builder_panel_standalone|default(false) ? null : url_for('admin.pages.builder', { id: page.id }),
  builder_view_live_url: page.status == 'published' ? url_for('public.page', { slug: page.slug }) : null,
  builder_empty_text: 'Add a hero, feature grid, FAQ, or rich text block. Legacy page body still renders below sections when present.',
  builder_list_id: 'page-builder-list',
  builder_root_id: 'admin-page-builder',
  section_rows: section_rows,
  section_palette_grouped: section_palette_grouped|default([]),
  section_labels: section_labels,
  section_icons: section_icons,
  builder_panel_standalone: builder_panel_standalone|default(false),
} %}
